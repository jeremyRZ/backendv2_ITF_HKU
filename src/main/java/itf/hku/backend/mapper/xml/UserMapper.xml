<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="itf.hku.backend.mapper.UserMapper">

    <insert id="insertSelective">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="name != null">name,</if>
            <if test="password != null">password,</if>
            <if test="post != null">post,</if>
            <if test="emailaddress != null">emailaddress,</if>
            <if test="cellnumber != null">cellnumber,</if>
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id,jdbcType=NUMERIC},</if>
            <if test="name != null">#{name,jdbcType=VARCHAR},</if>
            <if test="password != null">#{password,jdbcType=VARCHAR},</if>
            <if test="post != null">#{post,jdbcType=VARCHAR},</if>
            <if test="emailaddress != null">#{emailaddress,jdbcType=VARCHAR},</if>
            <if test="cellnumber != null">#{cellnumber,jdbcType=VARCHAR},</if>
        </trim>
    </insert>

    <select id="findOne" resultType="User">
        select * from user
        <where>
            <!-- 重复说一下，id 不能是 int，int 默认是 0，不可能是 null, 而 Integer 可以是 null-->
            <if test="id!=null">
                id=#{id}
            </if>
            <if test="name!=null">
                and name=#{name}
            </if>
        </where>
    </select>
</mapper>
